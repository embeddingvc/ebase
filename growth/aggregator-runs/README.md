# Aggregator run reports

One immutable file per scheduled run: `YYYY-MM-DD-<mode>.md`
(mode is `combined`, `discovery`, `drafting`, `review`, or `monthly`).

See `docs/github-aggregator-promotion.md` for the strategy and
`growth/aggregator-config.yaml` for limits. Do not edit past reports —
append a new dated file instead.
